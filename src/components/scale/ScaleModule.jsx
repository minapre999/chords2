import React, { useState, useEffect, useMemo } from "react";
import "./ScaleModule.css"

export default function ScaleModule() {

return (

    <div style={{ display: "flex", flexDirection: "column", gap: 20 }}>
        SCALE MODULE
    </div>
)
}